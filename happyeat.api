syntax = "v1"

type Request {
	Name string `path:"name,options=you|me"`
}

type Response {
	Message string `json:"message"`
}

@server(
	middleware: JWTMiddleware
)

service happyeat-api {
	@handler HappyeatHandler
	get /from/:name (Request) returns (Response)
}

