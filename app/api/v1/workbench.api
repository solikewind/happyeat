syntax = "v1"

info (
    title: "HappyEat工作台服务API"
    description: "HappyEat工作台服务相关接口"  
    usage: "工作台负责展示当前正在进行的订单，完成后点击‘制作完成’。"
    author: "Goxny"
    version: "v1.0.0"
)

@server (
	jwt:     Auth
	prefix:  /central/v1
	group:   workbench
	timeout: 5s
)

service WorkbenchService (
    @doc "列出工作台"
    @doc "创建工作台"
)